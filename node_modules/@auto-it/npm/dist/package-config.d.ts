export interface IRepoConfig {
    /** Owner of the repo (or GitHub user) */
    owner: string;
    /** The project */
    repo: string;
}
/** Try to the the owner/repo from the package.json */
export default function getConfigFromPackageJson(): Promise<IRepoConfig | undefined>;
//# sourceMappingURL=package-config.d.ts.map